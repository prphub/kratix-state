# This directory contains cluster-wide dependencies that need to be installed
# when the PostgreSQL Promise is installed.
# Currently empty as PostgreSQL doesn't require any cluster-wide resources.
